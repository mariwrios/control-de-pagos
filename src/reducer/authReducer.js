import { types } from "../types/types"


const initialState = {
  auth: false,
  user: false

}

export const authReducer = (state = initialState, actions) => {
  switch (types) {

 

  default:
    return state
  }
}
